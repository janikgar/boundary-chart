apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boundary-chart.fullname" . }}-test-connection"
  labels:
    {{- include "boundary-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boundary-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
